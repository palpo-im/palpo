services:
  # postgres:
  #   hostname: postgres
  #   image: postgres:18
  #   restart: always
  #   ports:
  #     - 5432:5432
  #   volumes:
  #     - ./data/pgsql:/var/lib/postgresql/data
  #   environment:
  #     POSTGRES_PASSWORD: ${PALPO_POSTGRES_PASSWORD:?set PALPO_POSTGRES_PASSWORD in examples/with-discord/.env}
  #     POSTGRES_USER: postgres
  #     POSTGRES_DB: palpo_local
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U postgres"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5
  #   networks:
  #     - internal

  matrix-bridge-discord:
    image: ghcr.io/palpo-im/matrix-bridge-discord:main
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./data:/data
    environment:
      CONFIG_PATH: "/data/config.yaml"
      RUST_LOG: matrix_bridge_discord=debug,info
    # depends_on:
    #   postgres:
    #     condition: service_healthy
